services:
  drupaleasy_repositories.commands:
    class: \Drupal\drupaleasy_repositories\Commands\DrupaleasyRepositoriesCommands
    arguments: ['@drupaleasy_repositories.service', '@entity_type.manager']
    tags:
      - { name: drush.command }
